name: Fábrica Turbo V36 (Fix Build & Discord) 🚀

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Baixando Código
        uses: actions/checkout@v3

      # --- 1. CONFIGURAÇÃO INICIAL ---
      - name: 🧐 Lendo Configurações
        id: config
        run: |
          APP_NAME="App Turbo"
          APP_ID="com.kaua.app"
          APP_VER="1.0.0"
          HAS_ICON="❌ Não"
          
          if [ -f "features.json" ]; then
            JSON_ID=$(jq -r '.packageId // empty' features.json)
            JSON_VER=$(jq -r '.version // empty' features.json)
            if [ ! -z "$JSON_ID" ]; then APP_ID=$JSON_ID; fi
            if [ ! -z "$JSON_VER" ]; then APP_VER=$JSON_VER; fi
          fi

          if [ -f "config.xml" ]; then
             XML_NAME=$(grep -oP '(?<=<name>).*?(?=</name>)' config.xml)
             if [ ! -z "$XML_NAME" ]; then APP_NAME=$XML_NAME; fi
          fi
          
          ICON_SOURCE=$(find www -maxdepth 1 -type f \( -iname "icon.png" -o -iname "logo.png" -o -iname "app-icon.png" \) | head -n 1)
          if [ -n "$ICON_SOURCE" ]; then HAS_ICON="✅ Sim"; fi
          
          echo "APP_NAME=$APP_NAME" >> $GITHUB_ENV
          echo "APP_ID=$APP_ID" >> $GITHUB_ENV
          echo "APP_VER=$APP_VER" >> $GITHUB_ENV
          echo "HAS_ICON=$HAS_ICON" >> $GITHUB_ENV

      # --- 2. DISCORD INÍCIO (AVISO) ---
      - name: 🔔 Discord (Início)
        if: env.DISCORD_WEBHOOK != ''
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
          -d "{
            \"username\": \"Fábrica Bot 🤖\",
            \"avatar_url\": \"https://img.icons8.com/fluency/96/robot-2.png\",
            \"embeds\": [{
              \"title\": \"🚧 Iniciando Produção\",
              \"description\": \"Criando: **${{ env.APP_NAME }}**\nID: \`${{ env.APP_ID }}\`\",
              \"color\": 16776960,
              \"fields\": [
                { \"name\": \"Versão\", \"value\": \"${{ env.APP_VER }}\", \"inline\": true },
                { \"name\": \"Ícone\", \"value\": \"${{ env.HAS_ICON }}\", \"inline\": true }
              ],
              \"footer\": { \"text\": \"Run #${{ github.run_number }}\" }
            }]
          }" \
          $DISCORD_WEBHOOK

      - name: ☕ Setup Ambiente
        uses: actions/setup-java@v3
        with: { distribution: 'temurin', java-version: '17' }

      - name: 🛠️ Setup Android
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;33.0.2" "platforms;android-33"
          npm install -g cordova

      # --- 3. CONSTRUÇÃO DO PROJETO ---
      - name: 🏗️ Criando App
        run: |
          cordova create app-temp com.temp.id "TempName"
          rm -rf app-temp/www/*
          cp -r www/* app-temp/www/
          cd app-temp
          
          # Configurações do config.xml
          sed -i "s|<name>.*</name>|<name>${{ env.APP_NAME }}</name>|g" config.xml
          sed -i "s/id=\"[^\"]*\"/id=\"${{ env.APP_ID }}\"/g" config.xml
          sed -i "s/version=\"[^\"]*\"/version=\"${{ env.APP_VER }}\"/g" config.xml
          
          # Permissões de arquivo
          sed -i '/<widget/a \    <preference name="AndroidPersistentFileLocation" value="Compatibility" />' config.xml
          sed -i '/<widget/a \    <preference name="AllowFileAccess" value="true" />' config.xml
          sed -i '/<widget/a \    <preference name="requestLegacyExternalStorage" value="true" />' config.xml

      - name: 🎨 Configurando Ícone
        run: |
          cd app-temp
          ICON_FILE=$(find www -maxdepth 1 -type f \( -iname "icon.png" -o -iname "logo.png" \) | head -n 1)
          sed -i '/<icon/d' config.xml
          
          if [ -n "$ICON_FILE" ]; then
            echo "🔥 Ícone encontrado!"
            cp "$ICON_FILE" ./icon.png
            sed -i '/<widget/a \    <icon src="icon.png" />' config.xml
          else
            echo "⚠️ Sem ícone."
          fi
          
          SPLASH_FILE=$(find www -maxdepth 1 -type f -iname "splash.png" | head -n 1)
          if [ -n "$SPLASH_FILE" ]; then
             cp "$SPLASH_FILE" ./splash.png
             sed -i '/<splash/d' config.xml
             sed -i '/<widget/a \    <splash src="splash.png" />' config.xml
             sed -i '/<widget/a \    <preference name="ShowSplashScreen" value="true" />' config.xml
          fi

      - name: 🔌 Instalando Plugins e Plataforma
        run: |
          cd app-temp
          
          # Plugins Base
          cordova plugin remove cordova-plugin-whitelist || true
          cordova plugin add cordova-plugin-ionic-webview
          cordova plugin add cordova-plugin-device
          cordova plugin add cordova-plugin-dialogs
          cordova plugin add cordova-plugin-android-permissions
          cordova plugin add cordova-plugin-inappbrowser
          cordova plugin add cordova-plugin-file
          cordova plugin add cordova-plugin-filepath
          cordova plugin add cordova-plugin-androidx-adapter

          INSTALLED_PLUGINS="Básicos"

          if [ -f "../features.json" ]; then
            USE_VIBRA=$(jq -r '.vibration' ../features.json)
            USE_CAM=$(jq -r '.camera' ../features.json)
            USE_GPS=$(jq -r '.geolocation' ../features.json)
            USE_BATTERY=$(jq -r '.battery' ../features.json)
            USE_FULL=$(jq -r '.fullscreen' ../features.json)
            USE_FLASH=$(jq -r '.flashlight' ../features.json)
            USE_MIC=$(jq -r '.microphone' ../features.json)
            USE_INSOMNIA=$(jq -r '.insomnia' ../features.json)
            USE_TOAST=$(jq -r '.toast' ../features.json)
            USE_FILE=$(jq -r '.file' ../features.json)

            if [ "$USE_VIBRA" = "true" ]; then cordova plugin add cordova-plugin-vibration; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Vibra"; fi
            if [ "$USE_CAM" = "true" ]; then cordova plugin add cordova-plugin-camera; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Cam"; fi
            if [ "$USE_GPS" = "true" ]; then cordova plugin add cordova-plugin-geolocation; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, GPS"; fi
            if [ "$USE_BATTERY" = "true" ]; then cordova plugin add cordova-plugin-battery-status; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Bat"; fi
            if [ "$USE_FULL" = "true" ]; then cordova plugin add cordova-plugin-fullscreen; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Full"; fi
            if [ "$USE_FLASH" = "true" ]; then cordova plugin add cordova-plugin-flashlight; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Luz"; fi
            if [ "$USE_TOAST" = "true" ]; then cordova plugin add cordova-plugin-x-toast; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Toast"; fi
            if [ "$USE_INSOMNIA" = "true" ]; then cordova plugin add cordova-plugin-insomnia; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Wake"; fi
            if [ "$USE_FILE" = "true" ]; then cordova plugin add cordova-plugin-file-opener2; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, File"; fi
            if [ "$USE_MIC" = "true" ]; then cordova plugin add cordova-plugin-media cordova-plugin-media-capture; INSTALLED_PLUGINS="$INSTALLED_PLUGINS, Mic"; fi
          fi
          
          echo "PLUGINS_LIST=$INSTALLED_PLUGINS" >> $GITHUB_ENV

          echo "📱 Adicionando Plataforma..."
          cordova platform add android@12.0.0
          
          # FIX: Força criação das pastas antes de tentar corrigir
          echo "🏗️ Forçando Prepare..."
          cordova prepare android || true
          
          echo "⏳ Aguardando sistema (10s)..."
          sleep 10
          
          # FIX: Reinstala dependências internas se a pasta existir
          if [ -d "platforms/android/cordova" ]; then
             echo "🚑 Reparando API Cordova..."
             cd platforms/android
             npm install --production --no-save
             cd ../..
          else
             echo "⚠️ Pasta cordova não encontrada. Tentando seguir..."
          fi

      - name: 🔨 Substituição de Ícones (Força Bruta)
        run: |
          cd app-temp
          if [ -f "icon.png" ]; then
             RES="platforms/android/app/src/main/res"
             # Só tenta copiar se a pasta res existir
             if [ -d "$RES" ]; then
                 for folder in mipmap-ldpi mipmap-mdpi mipmap-hdpi mipmap-xhdpi mipmap-xxhdpi mipmap-xxxhdpi drawable; do
                     mkdir -p "$RES/$folder"
                     cp icon.png "$RES/$folder/ic_launcher.png"
                     cp icon.png "$RES/$folder/icon.png"
                 done
             fi
          fi

      - name: 🚑 Fix Gradle
        run: |
          cd app-temp
          if [ -d "platforms/android" ]; then
              find platforms/android -name "*.gradle" -type f -exec sed -i 's/compile /implementation /g' {} +
              
              # Força SDK 33
              echo "cdvCompileSdkVersion=33" >> platforms/android/gradle.properties
              echo "cdvTargetSdkVersion=33" >> platforms/android/gradle.properties
              echo "cdvMinSdkVersion=24" >> platforms/android/gradle.properties
              echo "android.useAndroidX=true" >> platforms/android/gradle.properties
              echo "android.enableJetifier=true" >> platforms/android/gradle.properties
          fi

      - name: 📱 Build APK
        run: |
          cd app-temp
          rm -rf ~/.gradle/caches/
          echo "🚀 Compilando..."
          cordova build android --debug --packageType=apk -- --gradleArg=-PcdvCompileSdkVersion=33
          
          APK_PATH=$(find "$GITHUB_WORKSPACE" -type f -name "app-debug.apk" | head -n 1)
          if [ -z "$APK_PATH" ]; then
            echo "STATUS_BUILD=FALHA" >> $GITHUB_ENV
            exit 1
          else
            SAFE_NAME=$(echo "${{ env.APP_NAME }}" | iconv -f utf-8 -t ascii//TRANSLIT | sed 's/[^a-zA-Z0-9]/./g')
            mv "$APK_PATH" "$GITHUB_WORKSPACE/$SAFE_NAME.apk"
            echo "APK_FINAL=$SAFE_NAME.apk" >> $GITHUB_ENV
            echo "STATUS_BUILD=SUCESSO" >> $GITHUB_ENV
          fi

      - name: 🚀 Release GitHub
        if: success()
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: "${{ env.APP_NAME }} (v${{ github.run_number }})"
          body: |
            ✅ **App Pronto!**
            
            📱 **Nome:** ${{ env.APP_NAME }}
            🆔 **ID:** `${{ env.APP_ID }}`
            
            📥 **COMO BAIXAR:**
            Clique no arquivo `.apk` listado abaixo em **Assets**.
          files: ${{ env.APK_FINAL }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # --- 5. LOG FINAL (DISCORD COM DOWNLOAD) ---
      - name: 🔔 Discord (Relatório Final)
        if: always() && env.DISCORD_WEBHOOK != ''
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          STATUS: ${{ job.status }}
        run: |
          DL_LINK="${{ github.server_url }}/${{ github.repository }}/releases/tag/v${{ github.run_number }}"
          LOG_LINK="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          if [ "$STATUS" == "success" ]; then
            COLOR=5763712
            TITLE="✅ SUCESSO"
            CONTENT="@everyone 🎉 **${{ env.APP_NAME }}** está pronto!"
          else
            COLOR=15548997
            TITLE="❌ FALHA"
            CONTENT="@everyone ⚠️ Erro na produção de **${{ env.APP_NAME }}**"
          fi
          
          curl -H "Content-Type: application/json" \
          -d "{
            \"username\": \"Fábrica Bot 🤖\",
            \"avatar_url\": \"https://img.icons8.com/fluency/96/robot-2.png\",
            \"content\": \"$CONTENT\",
            \"embeds\": [{
              \"title\": \"$TITLE\",
              \"color\": $COLOR,
              \"fields\": [
                { \"name\": \"📱 App\", \"value\": \"${{ env.APP_NAME }}\", \"inline\": true },
                { \"name\": \"🆔 ID\", \"value\": \"\`${{ env.APP_ID }}\`\", \"inline\": true },
                { \"name\": \"🔌 Plugins\", \"value\": \"${{ env.PLUGINS_LIST }}\", \"inline\": false },
                { \"name\": \"📥 Download\", \"value\": \"[Baixar APK]($DL_LINK)\", \"inline\": true },
                { \"name\": \"📄 Logs\", \"value\": \"[Ver Erro]($LOG_LINK)\", \"inline\": true }
              ],
              \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"
            }]
          }" \
          $DISCORD_WEBHOOK
